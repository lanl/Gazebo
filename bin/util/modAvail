#!/usr/bin/perl

# determine if perl module is available
# example usage:  modavail <"mysql" or "DBD::mysql">

my $mod_name = shift;
chomp $mod_name;

  if(eval "use $mod_name; 1") {
      print "$mod_name is available\n"; 
  } else {
      print "$mod_name is NOT available\n"; 
  }
